# Time Logger Kotlin

A minimalist time tracking app written in Kotlin. Supports:

- Atomic event logging
- Local CSV export
- Dynamic category selection

## Build

Open in Android Studio and run on an Android device. Minimum SDK 21.